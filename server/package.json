{
  "name": "openmasjid-display-server",
  "private": true,
  "version": "0.31.1",
  "description": "Control plane + timetable renderer + embedded RTSP server for OpenMasjid Display",
  "license": "AGPL-3.0-only",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/prayer/engine.test.ts src/scheduler.test.ts"
  },
  "dependencies": {
    "@resvg/resvg-js": "2.6.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
